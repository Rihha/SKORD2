const typed = new Typed('.typing', {
    strings: ['Relax, unwind and have fun with SKORD.', 'Experience undending relieve with SKORD.'],
    typeSpeed: 100,
    backSpeed: 100,
    backDelay: 1000,
    loop: true
})